# NEXTWORKS 홈페이지 디자인 적용 가이드

## 1. 문서 역할

이 문서는 NEXTWORKS 홈페이지를 구현·수정할 때 선택하여 적용하는 **디자인 전용 기준서**입니다.

- 콘텐츠의 사실 여부와 문구는 `responsive_corporate_website_spec.md`를 원본으로 사용합니다.
- 이 문서는 레이아웃, 여백, 색상, 타이포그래피, 시각적 흐름만 결정합니다.
- 콘텐츠 기획서와 디자인 기준서가 충돌하면 사실 콘텐츠는 기획서를 우선하고, 시각 표현은 이 문서를 우선합니다.

## 2. 참고 화면 사용 원칙

| 참고 자료 | 허용 범위 | 금지 범위 |
| --- | --- | --- |
| `refer_file/reference_home.png` | 레이아웃, 섹션의 시각 흐름, 여백 밀도, 카드의 둥근 모서리, 밝은 배경과 오렌지 포인트의 대비 | 화면의 텍스트, 브랜드명, 로고, 수치, 이미지, 고객사, 문구를 복제하거나 NEXTWORKS의 사실 정보처럼 사용 |

참고 화면은 구성의 분위기를 파악하기 위한 자료입니다. 구현물은 NEXTWORKS의 승인된 콘텐츠와 제공된 서비스 이미지를 사용해야 합니다.

## 3. 시각 방향

### 분위기

- 밝고 절제된 B2B 기업 홈페이지
- 넓은 여백으로 메시지와 이미지의 우선순위를 분명하게 표현
- 짙은 차콜 텍스트와 오렌지 포인트로 상담 CTA와 중요한 순서를 강조
- 과도한 그래디언트, 숫자 카운터, 자동 재생 애니메이션, 장식용 아이콘 남용은 사용하지 않음

### 디자인 토큰

| 역할 | 값 | 적용 위치 |
| --- | --- | --- |
| 본문 배경 | `#FFFFFF` | 기본 섹션 |
| 보조 배경 | `#F6F7F9` | 히어로·서비스·문의 패널 |
| 본문 텍스트 | `#25282D` | 제목·본문·푸터 강조 |
| 보조 텍스트 | `#667085` | 설명·메타 정보 |
| 포인트 | `#FF6400` | CTA·단계 번호·라벨 |
| 경계선 | `#D9DEE7` | 카드·입력 필드·구분선 |
| 카드 모서리 | `20px` | 서비스 카드·이미지·문의 양식 |
| 콘텐츠 최대 폭 | `1200px` | 데스크톱 중앙 정렬 |

### 타이포그래피

- 폰트: `Pretendard, "Noto Sans KR", "Malgun Gothic", Arial, sans-serif`
- `h1`: `clamp(2.4rem, 4.2vw, 4.5rem)`, 굵기 700~800, 줄높이 1.15
- `h2`: `clamp(1.9rem, 3vw, 3rem)`, 굵기 700, 줄높이 1.25
- 본문: 최소 `16px`, 줄높이 1.7
- 제목은 자간을 약간 조여 정보 밀도를 높이고, 본문은 읽기 쉬운 줄높이를 유지

## 4. 레이아웃 원칙

1. **헤더**: 흰색 반투명 배경, 얇은 하단 경계선, 데스크톱 메뉴와 오렌지 또는 차콜 상담 CTA를 둡니다.
2. **히어로**: 넓은 보조 배경 위에 큰 메시지·2개 CTA·큰 이미지 프레임을 배치합니다. 데스크톱은 2열, 모바일은 1열입니다.
3. **정보 섹션**: 한 섹션에는 하나의 핵심 제목과 짧은 설명을 먼저 보여 준 뒤, 카드·목록·타임라인으로 세부 내용을 제공합니다.
4. **카드**: 서비스 이미지는 같은 비율로 자르고, 흰색 카드·얇은 경계선·20px 모서리로 통일합니다.
5. **흐름**: 진행 방식은 데스크톱에서 가로 순서, 모바일에서 세로 타임라인으로 전환합니다. 순서 번호는 반드시 텍스트로 제공합니다.
6. **문의**: 마지막에 대비가 분명한 문의 영역을 배치하며, 안내와 양식을 한 덩어리로 인식할 수 있게 구성합니다.

## 5. 여백과 반응형 기준

| 구간 | 콘텐츠 좌우 여백 | 섹션 상하 여백 | 레이아웃 |
| --- | --- | --- | --- |
| 1024px 이상 | 32px | 104px | 히어로 2열, 서비스 3열 |
| 768px~1023px | 32px | 88px | 서비스 2열, 진행 방식 줄바꿈 또는 세로 전환 |
| 767px 이하 | 20px | 72px | 핵심 콘텐츠 1열, 햄버거 메뉴 |
| 390px | 20px | 72px | 수평 스크롤 없이 양식·CTA 100% 폭 |

- 버튼과 모바일 메뉴 제어 영역은 최소 44px 높이를 유지합니다.
- 이미지에는 `object-fit: cover`를 사용하고 카드별 이미지 비율을 통일합니다.
- 카드·버튼 전환은 150~200ms로 제한하고, `prefers-reduced-motion` 환경에서는 최소화합니다.

## 6. 제작 시 적용 절차

1. `responsive_corporate_website_spec.md`에서 페이지 구조와 확정 콘텐츠·미확인 정보를 확인합니다.
2. 디자인 변경이 필요한 작업이면 이 `DESIGN-claude.md`를 선택하여 레이아웃·토큰·반응형 기준을 적용합니다.
3. 참고 화면의 콘텐츠를 복사하지 않았는지, 제공되지 않은 회사 정보나 실적을 추가하지 않았는지 검수합니다.
4. 1440px, 768px, 390px에서 여백·카드 열 수·텍스트 줄바꿈·수평 스크롤을 검수합니다.

## 7. 수정 요청 작성·처리 기준

디자인 또는 기능을 수정할 때는 의도하지 않은 변경을 줄이기 위해 아래 정보를 함께 기록합니다.

| 항목 | 작성 내용 | 예시 |
| --- | --- | --- |
| 수정 대상 | 문제가 발생한 메뉴, 버튼, 이미지, 문구 또는 레이아웃 | 모바일 히어로 제목 |
| 발생 위치 | 페이지 섹션과 화면 폭 또는 기기 | 메인 화면, 390px |
| 현재 문제 | 관찰 가능한 현상 | 제목이 3줄로 표시됨 |
| 원하는 변경 | 크기, 위치, 동작 방식 등 기대 결과 | 2줄 이내가 되도록 글자 크기와 줄 간격 조정 |
| 유지 조건 | 수정하지 않아야 하는 범위 | PC 화면의 레이아웃과 기존 색상 유지 |

### 수정 우선순위

1. 페이지가 열리지 않거나 리소스를 불러오지 못하는 실행 오류
2. 메뉴, 버튼, 링크, 양식 등 상호작용 오류
3. 글자·이미지 잘림, 콘텐츠 겹침, 가로 스크롤 같은 화면 오류
4. 정렬, 여백, 크기 등 디자인 오류
5. 색상, 표현, 전환 효과 등 세부 개선

### 처리 원칙

- 하나의 요소를 수정할 때에도 다른 화면 폭과 섹션의 정렬·동작에 영향을 주는지 함께 확인합니다.
- 요청에 유지 조건이 없으면, 변경 대상 외의 기존 콘텐츠·색상·레이아웃은 유지합니다.
- 오류가 반복되면 문제 화면의 캡처, 사용 브라우저, 화면 크기를 함께 기록합니다.
- 수정 후에는 최소한 데스크톱(1440px), 태블릿(768px), 모바일(390px)에서 재검증합니다.

### 요청 예시

> 모바일 화면에서 메인 제목이 세 줄로 표시됩니다. 390px 기준으로 두 줄 이내가 되도록 글자 크기와 줄 간격을 조정해 주세요. PC 화면의 레이아웃과 기존 색상은 변경하지 않습니다.

## 8. 출처

- `responsive_corporate_website_spec.md`의 2장, 5장, 6장, 8장: 콘텐츠 사용 원칙·기존 디자인·반응형·접근성 기준
- `C:/Users/user/Desktop/수업자료/refer_file_01/reference_home.png`: 레이아웃·여백·시각 흐름 참고. 콘텐츠 복제 금지
