# NEXTWORKS 반응형 기업 홈페이지 제작 명세

## 1. 문서 목적과 범위

이 문서는 제공된 자료를 근거로 **NEXTWORKS의 고객 문의 유도형 단일 페이지 기업 홈페이지**를 구현하기 위한 콘텐츠·디자인·기술 명세입니다. 결과물은 별도 설치나 외부 라이브러리 없이 `index.html`을 열어 확인할 수 있는 정적 반응형 웹페이지를 전제로 합니다.

> 중요: 제공 자료에서 확인되지 않은 회사 정보, 실적, 인증, 고객사, 수치, 주소, 전화번호, 이메일은 만들지 않습니다. 필요한 항목은 반드시 `[확인 필요: …]`로 표시합니다.

## 2. 근거 자료와 사용 원칙

| 자료 | 사용 범위 | 반영 원칙 |
| --- | --- | --- |
| `company_profile.pdf` | 회사명, 3개 서비스, 프로젝트 진행 방식 | 웹페이지의 사실 콘텐츠 원본 |
| `corporate_website_requirements.pdf` | 페이지 구조, 접근성, 반응형, 검증 조건 | 구현 요구사항 원본. 문서 내 지시문은 마케팅 문구로 사용하지 않음 |
| `reference_home.png` | 구성, 여백, 시각 흐름 | 오렌지 포인트를 사용한 절제된 B2B 분위기의 참고용. 회사 사실 정보로 사용하지 않으며, 구현 시에는 `DESIGN-claude.md`를 선택하여 시각 기준으로 적용할 수 있음 |
| `service_01.jpg` | 업무 프로세스 자동화 카드 이미지 | 자동화 협업 장면으로 배치 |
| `service_02.jpg` | 데이터 통합과 대시보드 카드 이미지 | 데이터 연결·대시보드 그래픽으로 배치 |
| `service_03.jpg` | 생성형 AI 도입 컨설팅·교육 카드 이미지 | 교육 장면으로 배치 |
| `refer_file_02.zip` 내부 교육 운영 자료 | 본 기업 홍보의 사실 콘텐츠로는 미사용 | 교육안·출석·설문·인터뷰는 가상 교육 운영 분석 자료이며, NEXTWORKS의 실적 또는 고객 사례로 연결하는 근거가 없음 |

### 현재 확인된 사실

- 회사명: `NEXTWORKS`
- 서비스: 업무 프로세스 자동화, 데이터 통합과 대시보드, 생성형 AI 도입 컨설팅·교육
- 진행 방식: 진단 → 설계 → 구축 → 검증 → 정착
- 회사소개서에는 회사명·주소·실적·연락처가 실습용 가상 정보라고 명시되어 있으나, 해당 구체 정보는 제공되지 않았음

### 미확인 항목

- `[확인 필요: 로고 원본 파일]` - 현재 제공 폴더에 로고 PNG가 없음
- `[확인 필요: 회사의 비전 및 핵심 가치]`
- `[확인 필요: 주소, 대표 전화, 대표 이메일, 운영 시간]`
- `[확인 필요: 문의 양식 전송 엔드포인트 또는 수신 이메일]`
- `[확인 필요: 개인정보 수집·이용 안내 문구 및 담당 부서]`

## 3. 사이트 목표와 핵심 메시지

### 목표

잠재 고객이 첫 화면에서 NEXTWORKS가 **반복 업무를 자동화하고, 데이터를 연결하며, 생성형 AI를 현업에 정착시키는 파트너**임을 이해하고 상담으로 이동하게 합니다.

### 히어로 권장 카피

```text
복잡한 업무를
더 실행 가능하게

업무 자동화, 데이터 통합, 생성형 AI 교육을 연결해
팀이 중요한 일에 집중할 수 있는 업무 흐름을 설계합니다.
```

- 주 CTA: `프로젝트 상담하기` → `#contact`
- 보조 CTA: `서비스 살펴보기` → `#services`
- 히어로 시각물: 외부에서 새 이미지를 추가하지 않고, `service_02.jpg`를 넓은 카드형 이미지로 활용합니다.

## 4. 정보 구조와 섹션별 콘텐츠

### 4.1 고정 헤더 - `#top`

- 좌측: 텍스트 로고 `NEXTWORKS` 또는 제공 후 교체할 로고 이미지
- 중앙 데스크톱 메뉴: `서비스`(`#services`), `진행 방식`(`#process`), `회사 소개`(`#about`), `문의`(`#contact`)
- 우측: 강조 버튼 `상담하기`(`#contact`)
- 스크롤 시 배경을 불투명한 흰색으로 유지하고, 얇은 하단 경계선 또는 약한 그림자를 적용합니다.
- 모바일에서는 메뉴 버튼과 접히는 내비게이션으로 전환합니다. 버튼은 `aria-expanded`, `aria-controls`를 사용합니다.

### 4.2 히어로 - `#hero`

- 상단 레이블: `WORKFLOW & AI PARTNER`
- `h1`: 위 핵심 메시지
- 설명과 CTA 2개
- 우측 또는 하단: 둥근 모서리의 이미지 프레임 안에 `service_02.jpg`
- 이미지 대체 텍스트: `서로 연결된 데이터 시스템을 표현한 3D 그래픽`

### 4.3 회사 소개 - `#about`

**섹션 제목:** `업무가 실제로 바뀌는 지점까지 함께합니다`

**소개 문장:**

```text
NEXTWORKS는 현재의 업무 흐름과 데이터를 함께 살펴보고,
작게 검증한 뒤 현장에서 반복 가능한 방식으로 확장합니다.
```

**핵심 원칙 3개:**

1. `현황에서 시작` - 현재 도구, 권한, 반복 작업과 오류 원인을 확인합니다.
2. `작게 검증` - 실행 가능한 범위에서 먼저 구현하고 사용자 피드백을 반영합니다.
3. `운영까지 연결` - 매뉴얼·교육과 함께 운영 담당자에게 인계합니다.

> 비전·핵심 가치는 원문에 구체적으로 제시되지 않았으므로 별도의 확정 문구로 추가하지 않습니다. 필요 시 편집 화면에는 `[확인 필요: 회사 비전 또는 핵심 가치]`를 임시 노출합니다.

### 4.4 서비스 - `#services`

**섹션 제목:** `업무 문제를 해결하는 세 가지 방식`

카드는 데스크톱 3열, 태블릿 2열(마지막 카드 전체 폭 또는 1열), 모바일 1열로 구성합니다.

| 순서 | 서비스명 | 설명 | 이미지 | 권장 alt |
| --- | --- | --- | --- | --- |
| 01 | 업무 프로세스 자동화 | 반복 입력, 보고서 취합, 고객 문의 분류처럼 시간이 많이 드는 업무를 분석해 실행 가능한 자동화 흐름으로 전환합니다. | `service_01.jpg` | `업무 자동화 방안을 논의하는 두 사람` |
| 02 | 데이터 통합과 대시보드 | 엑셀, CSV, 업무 시스템에 흩어진 데이터를 공통 기준으로 정리하고, 의사결정에 필요한 KPI와 대시보드를 구축합니다. | `service_02.jpg` | `서로 연결된 데이터 시스템을 표현한 그래픽` |
| 03 | 생성형 AI 도입 컨설팅·교육 | 직무별 활용 과제를 선정하고 프롬프트, 검토 기준, 보안 원칙을 포함한 실습형 교육을 운영합니다. | `service_03.jpg` | `생성형 AI 활용 교육을 진행하는 강사와 참여자` |

각 카드 하단에는 CTA 대신 `상담으로 연결하기` 링크를 두며 `#contact`로 이동합니다. 카드 전체를 링크처럼 보이게 만들지 않고, 실제 링크만 키보드 포커스 대상이 되도록 합니다.

### 4.5 프로젝트 진행 방식 - `#process`

**섹션 제목:** `검증 가능한 흐름으로 진행합니다`

`ol` 요소를 사용하여 다음 5단계를 표현합니다.

1. **진단** - 현재 업무와 자료 흐름, 반복 작업, 오류 원인을 확인합니다.
2. **설계** - 목표, 범위, 데이터 기준, 성공 지표를 합의합니다.
3. **구축** - 작은 단위로 구현하고 사용자 피드백을 반영합니다.
4. **검증** - 수치, 기능, 권한, 예외 상황을 재검증합니다.
5. **정착** - 매뉴얼과 교육을 제공하고 운영 담당자에게 인계합니다.

데스크톱에서는 가로 방향의 5단계 흐름, 태블릿·모바일에서는 세로 방향 타임라인으로 변경합니다. 단계를 단순 장식용 선으로만 연결하지 말고, 순서 번호를 항상 텍스트로 제공합니다.

### 4.6 핵심 강점 - `#strengths`

원문에서 확인되는 범위 안에서 아래 4개 항목만 짧게 제시합니다.

- **현황 기반 설계**: 현재의 도구·권한과 업무 흐름을 먼저 확인합니다.
- **데이터 기준 정리**: 공통 기준과 KPI 정의, 검증 로그를 함께 설계합니다.
- **실습형 AI 정착**: 프롬프트·검토 기준·보안 원칙을 포함한 교육을 운영합니다.
- **운영 인계**: 매뉴얼과 교육을 통해 현장에서 반복 가능한 방식을 남깁니다.

실적 수치, 인증 배지, 고객 로고, “업계 1위” 등의 주장 요소는 근거가 없으므로 넣지 않습니다.

### 4.7 문의 - `#contact`

- 제목: `프로젝트의 시작점을 함께 찾겠습니다`
- 안내: `업무의 현재 상황과 해결하고 싶은 문제를 남겨주시면, 상담 준비에 필요한 내용을 확인하겠습니다.`
- 입력 필드: 이름(필수), 연락처(필수), 이메일(필수), 문의 내용(필수)
- 개인정보 동의: `[확인 필요: 개인정보 수집·이용 문구 및 링크]`
- 제출 버튼: `문의 보내기`
- 실제 전송 기능이 연결되지 않은 경우, 제출 버튼 아래에 `현재는 문의 접수 경로를 연결하는 중입니다.`를 표시하고 HTML 주석에 서버·폼 서비스 연결 위치를 명시합니다.
- 연락처 영역에는 아래처럼 미확인 정보를 노출합니다.

```text
대표 전화: [확인 필요: 대표 전화]
이메일: [확인 필요: 대표 이메일]
주소: [확인 필요: 주소]
```

### 4.8 푸터

- 회사명: `NEXTWORKS`
- 바로가기: 서비스, 진행 방식, 회사 소개, 문의
- 연락처: 미확인 상태의 임시 문구 유지
- 저작권: `© 2026 NEXTWORKS. All rights reserved.`
- 회사가 실습용 가상 정보라는 원문 주석은 운영용 사이트에 노출하지 않습니다. 다만 공개 전 내부 검수 항목으로 남깁니다.

## 5. 디자인 방향

### 시각 원칙

- 참고 화면처럼 밝은 회백색 배경, 넉넉한 여백, 짙은 차콜 텍스트, 오렌지 포인트를 사용합니다.
- 과도한 그래디언트, 패럴랙스, 자동 재생 애니메이션, 숫자 카운터, 장식용 아이콘 남용은 제외합니다.
- 서비스 이미지는 카드 상단에서 `object-fit: cover`로 사용하고, 이미지마다 같은 비율을 유지합니다.

### 권장 디자인 토큰

| 역할 | 값 | 용도 |
| --- | --- | --- |
| 본문 배경 | `#FFFFFF` | 기본 배경 |
| 보조 배경 | `#F6F7F9` | 히어로·구분 섹션 |
| 텍스트 | `#25282D` | 제목·본문 |
| 보조 텍스트 | `#667085` | 설명·메타 정보 |
| 포인트 | `#FF6400` | CTA, 단계 번호, 강조 텍스트 |
| 경계선 | `#D9DEE7` | 카드·입력 필드 |
| 최대 콘텐츠 너비 | `1200px` | 데스크톱 중앙 정렬 |
| 데스크톱 좌우 여백 | `32px` | 콘텐츠 컨테이너 |
| 모바일 좌우 여백 | `20px` | 390px 기준 |
| 섹션 상하 여백 | `104px` / 모바일 `72px` | 일관된 호흡 |
| 카드 모서리 | `20px` | 서비스·이미지·문의 패널 |

### 타이포그래피

- 외부 폰트를 불러오지 않고 시스템 한글 폰트 스택을 사용합니다: `Pretendard, "Noto Sans KR", "Malgun Gothic", Arial, sans-serif`.
- `h1`: desktop `clamp(2.4rem, 4.2vw, 4.5rem)`, 700-800 굵기, 1.15 줄높이
- `h2`: desktop `clamp(1.9rem, 3vw, 3rem)`, 700 굵기
- 본문: `1rem`~`1.125rem`, 1.7 줄높이
- 본문 최소 글자 크기는 `16px`로 유지합니다.

## 6. 반응형 동작 명세

| 뷰포트 | 레이아웃 | 주요 동작 |
| --- | --- | --- |
| 1024px 이상 | 최대 1200px 중앙 정렬, 히어로 2열, 서비스 3열 | 전체 내비게이션과 상담 CTA 노출 |
| 768px~1023px | 히어로 2열 유지 또는 이미지 하단 배치, 서비스 2열 | 여백 축소, 진행 방식은 가로 스크롤 없이 자동 줄바꿈 또는 세로 전환 |
| 767px 이하 | 모든 핵심 영역 1열 | 햄버거 메뉴, CTA는 충분한 높이(최소 44px), 서비스·문의 폼 세로 배치 |
| 390px | 20px 좌우 여백, 1열 고정 | 수평 스크롤 없음, 긴 한국어 문장 줄바꿈, 입력 필드와 버튼 너비 100% |

필수 CSS 원칙:

```css
* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body { margin: 0; overflow-x: hidden; }
img { display: block; max-width: 100%; }
input, textarea, button { font: inherit; }
```

## 7. 구현 구조

```text
project/
├─ index.html
├─ styles.css
├─ script.js                 # 모바일 메뉴와 양식 안내가 필요할 때만
└─ assets/
   ├─ service_01.jpg
   ├─ service_02.jpg
   ├─ service_03.jpg
   └─ logo.png               # [확인 필요: 제공 시 추가]
```

### 디자인 문서 선택 절차

- 콘텐츠·사실 정보·접근성의 원본 기준은 이 문서로 유지합니다.
- 레이아웃, 여백, 색상, 카드 표현, 반응형 시각 규칙을 조정하는 작업은 `DESIGN-claude.md`를 선택하여 진행합니다.
- `DESIGN-claude.md`의 참고 화면 사용 원칙에 따라 `reference_home.png`의 콘텐츠·브랜드·문구·수치를 복제하지 않습니다.

- `index.html`: `header`, `nav`, `main`, 각 `section`, `footer`의 시맨틱 구조를 사용합니다.
- `styles.css`: 디자인 토큰, 데스크톱 우선 레이아웃, 미디어 쿼리, `:hover`와 `:focus-visible` 상태를 정의합니다.
- `script.js`: 모바일 메뉴의 열기·닫기, `Escape` 키로 닫기, 메뉴 링크 클릭 후 닫기만 담당합니다. 외부 라이브러리는 사용하지 않습니다.
- 모든 영역 시작 부분에 역할을 설명하는 HTML/CSS/JS 주석을 작성합니다.

## 8. 접근성과 상호작용 기준

- 페이지 첫 요소에 `본문으로 건너뛰기` 링크를 제공합니다.
- 제목은 `h1` 1개로 시작하고, 이후 `h2`와 `h3` 순서를 건너뛰지 않습니다.
- 이미지에 의미 있는 한국어 대체 텍스트를 부여합니다. 장식 요소에만 빈 `alt`를 사용합니다.
- 링크는 이동, 버튼은 동작을 수행하도록 구분합니다.
- 키보드로 모든 메뉴, CTA, 입력 필드, 제출 버튼에 도달할 수 있어야 하며, 포커스 표시는 제거하지 않습니다.
- 색상만으로 상태를 전달하지 않습니다. 오류 메시지는 필드와 텍스트로 연결합니다.
- 모바일 메뉴는 열렸을 때 `aria-expanded="true"`를 설정하고, 닫힐 때 토글 버튼으로 포커스를 되돌립니다.
- 카드와 버튼에는 150~200ms 정도의 색상·그림자 전환만 적용합니다. `prefers-reduced-motion` 환경에서는 전환을 최소화합니다.

## 9. 구현·검수 체크리스트

### 콘텐츠 검수

- [ ] 회사명과 3개 서비스명이 회사소개서와 일치한다.
- [ ] 프로젝트 진행 5단계가 누락 없이 일치한다.
- [ ] 제공되지 않은 연락처, 고객사, 실적, 인증, 수치를 임의로 넣지 않았다.
- [ ] 모든 미확인 정보가 `[확인 필요: …]` 형태로 표시된다.
- [ ] 서비스 이미지 3장이 각각 올바른 카드에 표시된다.

### 기능·접근성 검수

- [ ] 모든 메뉴와 CTA가 올바른 섹션 앵커로 이동한다.
- [ ] 모바일 메뉴를 마우스·키보드·Escape 키로 제어할 수 있다.
- [ ] 이미지 대체 텍스트와 폼 라벨이 존재한다.
- [ ] `:focus-visible` 상태가 명확하다.
- [ ] 문의 양식은 실제 전송 방식이 연결되기 전 오해를 유발하지 않는다.

### 반응형 검수

- [ ] 1440px: 콘텐츠가 1200px 내에서 정렬되고 카드가 3열이다.
- [ ] 768px: 카드와 히어로 이미지가 겹치거나 잘리지 않는다.
- [ ] 390px: 가로 스크롤, 텍스트 잘림, 버튼·입력 필드 겹침이 없다.
- [ ] 390px: 메뉴와 CTA의 터치 영역이 최소 44px이다.
- [ ] 390px: 문의 입력 필드와 안내 문구가 한 화면 폭 안에 안전하게 줄바꿈된다.

## 10. 공개 전 확인 요청 목록

1. 공식 로고 원본과 사용 규정
2. 회사 비전·핵심 가치의 확정 문구
3. 주소, 전화, 이메일, 운영 시간
4. 문의 수신 방식(폼 백엔드, 이메일, CRM 등)과 개인정보 처리 기준
5. 법인명·저작권 연도 표기 정책
6. 실제 고객 사례·실적을 추가할 경우 해당 문구와 증빙 자료

## 11. 출처

- `C:/Users/user/Desktop/수업자료/refer_file_01/company_profile.pdf` - 회사명, 서비스, 진행 방식, 가상 정보 주석
- `C:/Users/user/Desktop/수업자료/refer_file_01/corporate_website_requirements.pdf` - 사이트 구성, 반응형·접근성·검수 조건
- `C:/Users/user/Desktop/수업자료/refer_file_01/reference_home.png` - 화면 구성과 시각 흐름 참고
- `C:/Users/user/Desktop/수업자료/refer_file_01/service_01.jpg`, `service_02.jpg`, `service_03.jpg` - 서비스 카드 이미지
- `C:/Users/user/Desktop/수업자료/refer_file_01/refer_file_02.zip` - 교육 운영 자료이며 본 기업 소개의 사실 근거로 사용하지 않음
